import 'package:flutter/material.dart';

class AppColors {
  /// usage:
  /// `AppColors.white`
  static const Color white = Color(0xffFFFFFF);

  /// usage:
  /// `AppColors.black`
  static const Color black = Color(0xff111111);

  /// usage:
  /// `AppColors.primary[50]`
  ///  `AppColors.primary.shade.50`

  static const Color backgroundBlue = Color(0xff115377);
  static const Color lightBlue = Color(0xff02A8F0);
  static const Color neonBlue = Color(0xFF64FFE3);
  static const Color textColorBlack = Color(0xff12141B);
  static const Color inactiveSlider = Color(0xff064D43);
}
